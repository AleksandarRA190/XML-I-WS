import { AccommodationpipePipe } from './accommodationpipe.pipe';

describe('AccommodationpipePipe', () => {
  it('create an instance', () => {
    const pipe = new AccommodationpipePipe();
    expect(pipe).toBeTruthy();
  });
});
